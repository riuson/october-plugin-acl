# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    name:
        label: riuson.acl::lang.backend_group.name
        searchable: true
    description:
        label: riuson.acl::lang.backend_group.description
        searchable: true
    level:
        label: riuson.acl::lang.backend_group.level
        type: number
